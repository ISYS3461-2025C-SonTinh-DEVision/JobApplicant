spring.application.name=JobApplicant

# https
# server.port=${SERVER_PORT}
# server.ssl.enabled=true
# server.ssl.key-store=classpath:devcert.p12
# server.ssl.key-store-password=password
# server.ssl.key-store-type=PKCS12
# server.ssl.key-alias=devcert

# Multipart file upload configuration
# spring.servlet.multipart.max-file-size=5MB
# spring.servlet.multipart.max-request-size=10MB


# server.address=0.0.0.0

# ==============================================
# Spring Boot Actuator (Service Discovery Health Checks)
# Required for Consul and Kong Gateway health monitoring
# ==============================================
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
management.health.defaults.enabled=true

# Service metadata for discovery
info.app.name=JobApplicant Backend
info.app.description=DEVision Job Applicant API Service
info.app.version=1.0.0